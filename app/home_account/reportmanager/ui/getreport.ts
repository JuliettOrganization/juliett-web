// import { fetchFilteredReports } from '@/app/lib/data';
// import { sql } from '@vercel/postgres';
// import {Report} from '@/app/lib/definitions'

// export async function getReports(query: string, currentPage: number): Promise<Report[]> {
//   return await fetchFilteredReports(query, currentPage);
// }
